{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "start": "func start",
    "build": "echo 'Building API' && npm install",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "@azure/functions": "^4.0.0"
  }
}
